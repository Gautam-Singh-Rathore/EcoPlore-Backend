spring:
  application:
    name: Backend
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
#    show-sql: true
  devtools:
    restart:
      enabled: false
  mail:
    host: smtp.gmail.com
    port: 587
    username: infogreenplore@gmail.com
    password: xcvcnwrblfmqgsak
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
logging:
  level:
    root: info

rzp:
  key-id: rzp_live_RCemjVt0zfY8v2          # Use environment variable
  key-secret: Os5ZmY05N1NT3Ck3YQ7uZnDZ   # Use environment variable
  currency: INR
  company-name: MyCompany

nimbus:
  username: infogreenplore@gmail.com
  password: Delhi@gp_12

frontend-url: ${FRONTEND_URL:http://localhost:5173}